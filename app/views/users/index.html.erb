<div class="ls-box-group">
  <div class="ls-box ls-md-space">
    <div class="row">
      <div class="col-md-2 ls-txt-center">
      <span class="ls-ico-alone ls-ico-users"></span>
      </div>
    <div class="col-md-10">
      <% if @users.empty? %>
        <p><%= 'Nenhum usuário cadastrado' %></p>
      <% end %>
      <ul>
        <% @users.each do |user| %>
          <li><p style="font-size: 20px;"> <%= link_to user.id, user %>
            <%= "- #{user.name}" %> </p></li>
        <% end %>
      </ul>
    </div>
  </div>
  </div>
</div>
